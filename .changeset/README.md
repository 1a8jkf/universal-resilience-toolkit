# Changesets

Run `npm run changeset` for user-facing changes. Choose the semver bump and describe
the observable behavior in English. Commit the generated Markdown file with the code.
The release workflow creates a version PR and publishes after merge when enabled.
